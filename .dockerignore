node_modules
**/node_modules

# Exclude other artifacts (not needed for api-server)
artifacts/riden-admin
artifacts/riden-captain
artifacts/riden-passenger
artifacts/riden-admin-app
artifacts/riden-captain-app
artifacts/riden-passenger-app
artifacts/riden-promo-video
artifacts/mockup-sandbox

# Exclude pre-built dist (we build from source now)
artifacts/api-server/dist

# Exclude lib packages not needed by api-server
lib/api-client-react
lib/api-spec

# Misc
attached_assets
.git
*.log
